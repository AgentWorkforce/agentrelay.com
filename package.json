{
  "name": "agentrelay.com",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=20.9.0"
  },
  "workspaces": [
    "web",
    "router"
  ],
  "scripts": {
    "dev": "npm --workspace web run dev",
    "build": "npm --workspace web run build",
    "typecheck": "tsc --noEmit -p web/tsconfig.json && npm --workspace router run typecheck",
    "lint": "oxlint web router",
    "test": "npm --workspaces run test",
    "preview": "npm --workspace web run preview",
    "deploy": "npm --workspace web run deploy",
    "deploy:router": "npm --workspace router run deploy"
  },
  "devDependencies": {
    "oxlint": "1.76.0"
  }
}
