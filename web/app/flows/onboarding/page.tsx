import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Create your first software factory',
  description: 'Choose your coding agent and build a flow that opens PRs, reviews its work, and waits for your approval.',
  robots: { index: false, follow: true },
};

export default function FlowOnboardingPage() {
  return null;
}
