---
title: 'Quickstart'
description: 'Run one issue through Factory end to end, in dry-run first.'
---

{/* Keep this to the shortest path that produces a real PR. Suggested spine:
    1. install / point at a repo
    2. a minimal factory.config.json
    3. `factory run-once --dry-run`
    4. `factory run-once` for real
    Everything else belongs on Configuration. */}

## 1. Configure

{/* Minimal factory.config.json — the smallest thing that works. */}

## 2. Dry run

```bash
npx @agent-relay/factory run-once --dry-run
```

## 3. Run it

{/* What a real run does and where the output shows up (PR, Linear, Slack). */}

## Next

<CardGroup cols={2}>
  <Card title="Safety scope" href="/docs/factory/safety-scope">
    {/* one line */}
  </Card>
  <Card title="Configuration" href="/docs/factory/configuration">
    {/* one line */}
  </Card>
</CardGroup>
