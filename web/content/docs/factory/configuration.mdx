---
title: 'Configuration'
description: 'factory.config.json — repo routing, issue source, models, states, and writeback.'
---

{/* One page for the config file. Resist documenting every field — link the
    schema for the long tail and cover the ones people actually set. */}

## Use the skill

Don't hand-write the config from memory — install the skill and let your coding agent author and validate it:

```bash
# with prpm
npx prpm install @agent-relay/factory-config

# with skills.sh
npx skills add https://github.com/agentworkforce/skills --skill factory-config
```

{/* Then ask it to "create a factory config for <repo>". The skill covers repo
    routing, Linear states and teams, GitHub issue ingestion, Slack, babysitter
    mode, and where the Relayflows dispatch wiring boundary sits. */}

## Repo routing

{/* The compact form (org + names + cloneRoot) vs the explicit form
    (byLabel / byProject / keywordRules / default). Label → repo → clone path. */}

## Issue source

{/* linear | github, and what changes between them. */}

## Models and roles

{/* implementer / reviewer / triage / babysitter, and which CLI backs each. */}

## Linear states

{/* Semantic roles (readyForAgent, agentImplementing, humanReview, done) and how
    they resolve to real states. Per-team overrides. */}

## Writeback

{/* Where status goes: Linear, Slack, GitHub comments and labels. */}
